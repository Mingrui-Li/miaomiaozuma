// These generators encode the rejected 20×5 mapping and must not resume.
// Remove this guard only as part of a reviewed replacement that reads approved
// per-level geometry, archives rejected V2 nodes and has explicit visual QA.
throw new Error('已冻结：旧版 20 模板 × 5 关 Figma 批量流程已被否决。先完成 docs/TRACK_REDESIGN_REVIEW.md 的逻辑与视觉门禁；禁止恢复此脚本或以 --replace 删除旧稿。');
