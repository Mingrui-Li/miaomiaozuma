# 美术状态

当前 ImageGen、Figma 与 Cocos 视觉实现全部暂停。

- `concepts/`、`final/`、`qa/` 中的既有文件均为历史尝试，不是已批准的开发基线。
- 被明确否决的资源移入 `archive/rejected/`，只保留作反例和追溯。
- 项目所有者确认 `docs/CORE_GAMEPLAY_SPEC.md` 和 20 条黑白路径拓扑前，禁止继续生成或写入正式视觉资源。
- 恢复视觉阶段后必须按 ImageGen → Figma V2 可编辑复现 → 项目所有者验收 → Cocos 的顺序进行。
