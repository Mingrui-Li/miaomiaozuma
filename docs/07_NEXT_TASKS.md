# 07 · 下一阶段任务与多窗口交接

更新：2026-09-08。总控窗口维护授权与优先级；**任务列表不是开工许可**。本轮仅 MEM-01 文档任务；其余均未开始。

## 状态与认领

使用 `待授权 / 可认领 / 进行中 / 待验收 / 完成 / 阻塞 / 暂停`。每个任务认领时填：窗口标识、分支或工作目录、文件白名单、开始日期、验收条件。共享目录没有自动锁；认领前和落盘前重新读取任务表与 Git 状态。

总控负责全局决策、任务划分、冲突协调；执行窗口一次只做一个明确任务，返回文件与证据。不同窗口可做不重叠任务，但不要同时改同一场景、Prefab、`.meta` 或共享记忆段落。冲突先协调，不覆盖；状态过期先确认原窗口停止，再接手，不凭时间自动抢占。

## 已完成任务

| ID | 任务 | 状态/负责人 | 修改范围 | 验收 |
|---|---|---|---|---|
| MEM-01 | 建立长期项目记忆，标明旧流程失效 | 完成 / 总控；本轮认领已释放 | AGENTS、00～07、README 与相关旧文档的状态提示/GDD 流程 | 真实结构核对；文档链接与流程一致性检查；不改功能/几何/素材 |

## UI 下一阶段（依赖顺序）

| ID | 任务 | 前置/状态 | 交付与验收 |
|---|---|---|---|
| UI-01 | 确定新 UI 生产路线及唯一视觉基线 | 待授权；负责人未分配 | 简短比较可复用素材直用、布局数据、可选 ImageGen/Figma 的职责；所有者确认一条路线与试样权限，写 D 日志，不开始整套重构 |
| UI-02 | 收敛页面导航与状态清单 | UI-01；待授权 | P00～P11、首关/常规准备、100 关结束、暂停/退出确认、免费/广告复活、无广告/取消/弱网/后台恢复逐项定义；先收敛 Q004，不增加系统 |
| UI-03 | 新视觉小样与基础令牌 | UI-01/02；待授权 | 配色、授权字体候选、五猫辨识、按钮、弹窗、HUD、图标；检查常规/窄屏/长屏安全区与居中，明确选用和否决项 |
| UI-04 | 生产素材清单与组件状态 | UI-03；待授权 | 按 04 记录源文件、导出、锚点、九宫格、文字、图标、各种状态；有复用来源，不依赖二次手描 |
| UI-05 | 代表页面的端到端视觉验证 | UI-03/04 且明确获准引擎试样；待授权 | 首页、首关、一个紧凑后期关、胜利/失败，用同一组件和资源验证新流程；不以此提前重构全部游戏。截图/触控/文字/几何叠图有证据 |
| UI-06 | 全页面与全部交互状态覆盖 | UI-05 通过；待授权 | 地图、图鉴/详情、外观、设置、加载/错误、提示等全部覆盖；按钮无死路，取消/禁用/加载状态齐全 |
| UI-07 | 百关视觉适配与逐关 QA | UI-05 且组件稳定；待授权 | 使用当前源数据检查 100 关；前后期尺寸、洞口方向、下一只不压轨、HUD/底部安全区、障碍可辨逐关记录；共享主题可以复用，不重画几何 |
| UI-08 | 视觉交付验收与实现任务拆分 | UI-06/07；待授权 | 冻结资产/布局/字体/组件版本、缺陷清单与批准记录，再由总控安排完整 Cocos 开发；不以图片完成宣称上线 |

UI-01 可以推荐“同一素材/参数减少转绘”，但不能未经确认把“Cocos 原生直搭”写成已决定。各任务的具体文件白名单待认领时确定；不创建 V3。

## 保留但不在本轮执行的技术任务

- TECH-01：按 06 的 R01～R08 制定旧运行代码与新规格的差距修复顺序，先收敛 Q003。
- TECH-02：百关几何到运行配置的接入、生成计划和存档上限/迁移；不复制旧 24 关映射。
- TECH-03：中央首撞、插入、动态回吸、暴走、胜负时序及测试。
- TECH-04：无限广告复活事务、快照恢复、真实抖音能力与生命周期异常验证。
- QA-01：百关动态模拟/人工体验、真机、多尺寸、资源包体/性能、上线检查。

上述均待授权；动态玩法缺口可以影响最终上线验收，但不得谎称为几何未设计完成。

## 新窗口启动话术（可复制）

> 请在《喵喵回窝》仓库工作，先读 AGENTS.md 和 docs/00～07 的项目记忆，再核对真实文件。本窗口只执行【任务 ID 与明确目标】，允许修改【文件范围】，禁止【本次排除项】。当前旧 ImageGen→Figma→Cocos 流程已停止采用，不自动续跑旧任务。先复述现状、边界与验收方式，认领任务后执行；完成时更新状态和交接记录，说明测试与未完成项。

如果用户只说“继续”，先从当前状态与已授权任务恢复；不存在有效认领或可执行授权时，报告最小下一步并请求范围，不猜测启动新功能。

## 交接记录格式

```text
任务 ID / 窗口标识 / 日期：
状态与授权来源：
分支、工作目录、认领文件范围：
已完成（附文件与证据）：
修改/新增文件与未提交情况：
验证命令、结果、未验证项：
新决策 ID / 待决问题：
最后成功步骤 / 未完成与失败原因：
进程、预览端口或临时资源（仅记录本任务创建的）：
下一条安全操作 / 不要重复的动作：
认领释放或需协调的文件：
```

### MEM-01 交接 · 2026-09-08

总控已完成项目记忆文档；仅文档落盘，未新增功能、未生图、未写 Figma。百关几何只读测试 139 项通过；核心/引擎/平台未测试。未提交 Git，保留原工作区。没有为本任务新开服务或后台任务。下一安全操作是与所有者确定 UI-01；不要续跑旧图像队列或自动重构。无活动文件认领。
