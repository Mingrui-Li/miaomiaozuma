# UI-05 · Cocos 组件与安卓验收版本

2026-09-11，总控 `/root`。D018授权代表页面及组件；D019确认首页入口和安卓测试设备。当前为本地验证阶段，手机与所有者视觉批准仍待完成。

**2026-09-14验收更新：** 所有者明确回复“很好，验收通过。请继续执行下一阶段任务”（D021），UI-05按所有者反馈验收通过，进入 [UI-06](15_UI_FLOW_PREVIEW.md)。未提供手机型号或原始截图，不补造测量。临时LAN服务PID26854已核对命令后关闭，下方旧LAN地址不再服务；历史本地验证证据保留。

## 本次产物

- 独立场景：`assets/ui-preview/ui-preview.scene`，UUID `85a79628-1393-46ff-b8e4-606992e3e345`。
- 真实Prefab：`assets/game/prefabs/ui/CommonButtonView.prefab`、`DialogPanel.prefab`、`GameHud.prefab`、`HomeView.prefab`。按钮、面板、HUD脚本在 `assets/game/scripts/ui/`；尺寸、标签与面板内容动态配置，场景通过Prefab引用实例化。
- 代表页面：首页、七个代表关HUD、暂停/重开确认/退出确认、结算、复活/加载/不可用、基础设置。首页按D019实现；关卡/图鉴/外观只有可返回的入口说明，分享显示不可用态，后续UI-06与平台任务绑定。
- 同源复用UI-04的12张PNG，未重新绘制、重新抠图或改动既有meta。轨道、下一只选择、手势归属继承UI-03G/04只读布局数据；源几何没有变化。
- 按钮状态包含正常、按下、禁用、加载；禁用/加载仍拦截点击，取消与免费重开/退出保留。开发工具栏提供广告完成/取消/失败模拟，未接真实广告、存档、奖励、音效或震动。
- 节点按名字复用，显隐切换；30次暂停开合没有增加已分配节点。该数据只证明本次复用路径，不是性能或内存泄漏全面验收。

## 字体与资源

Noto Sans SC VF 2.004，取500字重、仅保留本场景文案和ASCII，改名为 **MMHW UI Trial**。试样TTF 63,008字节；本批12PNG与字体合计393,063字节，不代表完整构建包体。子集cmap覆盖记录在 `design/ui/ui05/font.json`，源码及许可位于 `art/source/ui05/`，随运行字体附带OFL许可。

来源：[Noto官方字体](https://github.com/notofonts/noto-cjk/blob/main/Sans/Variable/TTF/Subset/NotoSansSC-VF.ttf)、[官方许可](https://github.com/notofonts/noto-cjk/blob/main/Sans/LICENSE)。完整源字体不进入游戏assets；源文件哈希记录固定实际版本。后续新增文案必须检查覆盖，不能把本批子集当作全游戏字体。

新生图仍受D017约束：后续须为可核实的GPT Image 2.5通道。本批没有调用型号未知的内置生图，也未调用付费API。

## 浏览器验证

使用Playwright CLI实际操作Cocos Canvas，脚本 `tools/ui-preview/browser-check.js`，结果 `design/ui/ui05/browser-results.json`，截图 `output/playwright/ui05/`。

- 代表关1/20/21/22/24/87/100，资源及Prefab成功装载，中文字体引用存在。
- 360×640、390×844、430×932，每个尺寸重新加载，测试模拟安全区；产品热区最小44 CSS px、互不重叠，透明边缘可触达。工具栏也扩大至44 CSS px高度，属于测试版界面。
- 首页三入口、设置返回、声音显示切换、禁用/加载开始按钮、结算分享不可用与返回、暂停和重开取消、道具取消、弹窗不透传、免费复活上限、广告模拟重复完成/取消/失败及免费出口通过。
- 30次暂停开合，复用集合98个节点保持稳定；最终浏览器diagnostics为空。

命令：Creator自带 `typescript/bin/tsc --noEmit`；`node --test tools/ui-greybox/layout.test.mjs`；Playwright CLI `run-code`读取上述脚本。几何/输入单测只验证适用纯逻辑，不能替代页面与手机验证。

## 打包与手机验收

构建由 `tools/ui-preview/prepare-build.py`复制必要依赖到全新的 `/private/tmp/mmhw-ui05-build-20260911`，避免与已打开工程冲突。CLI使用Creator3.8.8，web-mobile、该独立场景；退出36并成功产出，构建日志末尾worker SIGTERM为结束子进程记录。最终打包页面同样通过七关/三尺寸/状态/30次开合回归，diagnostics为空，见 `design/ui/ui05/build-browser-results.json`。

静态构建约11.2 MiB，包含调试引擎模块，尚未做正式抖音主包优化。浏览器模板缺省favicon产生过一次404，补充空data favicon后重测通过；另附OFL与字体版权通知。最终静态版本保存在 `build/ui05-verified/`（Git忽略），源码和构建哈希记录在 `design/ui/ui05/verification.json`。

**2026-09-14局域网入口已核验：[安卓测试版](http://10.40.93.139:62290/)。** D020明确授权临时同Wi-Fi开放，先前共享授权缺口已解决。服务PID26854，仅提供 `build/ui05-verified/`；57文件经本机通过LAN地址读取均HTTP200且与验收SHA一致，无符号链接，见 `design/ui/ui05/network-20260914.json`。这证明服务和内容可用，尚未证明手机网络连通或完成真机验收。本机旧入口127.0.0.1:62290仍保留。

安卓手机与电脑连接同一Wi-Fi，在手机浏览器打开上述地址。此入口是浏览器测试版，尚不是抖音小游戏容器实测；不需要安装APK。保持电脑联网和测试服务运行，测试结束通知总控关闭临时LAN服务。地址在电脑换网后可能变化，打不开时先报告具体提示，不自行更改防火墙或路由设置。

手机检查：

1. 点首页，查看标题、小猫、开始按钮与三个次入口；截一张完整屏幕。
2. 开始后点暂停；点面板外不应发射，继续按钮应易点。检查顶部与系统挖孔、底部手势区是否冲突。
3. 用底部换关到第87关，检查下一只、道具和轨道是否挤压；黑猫56px是否能认出轮廓和脸。
4. 点安全区测试最紧布局；点胜利/复活，检查长文案、按钮边缘和免费出口。广告模拟不是真广告。
5. 返回手机桌面再回来、短暂切换横竖屏，记录恢复和触达异常；这部分尚未由桌面尺寸测试覆盖。

回传手机型号、浏览器、上述首页/第87关/弹窗截图，以及是否有遮挡、难点、缺字或视觉偏好。收到真机证据和所有者具体审美反馈后，才决定UI-05通过及UI-06推进。

## 保留限制

不是完整可玩百关，不修复旧main的历史规则，不实现奖励/广告/分享。场景“下一关”只切换七个代表关样本，不能据此验收关卡推进。入口/老巢/发射器仍为结构示意图形，猫只有静态帧；头像动画、奖励内容完整呈现、正式容器安全区及性能/启动包体留待相应任务。此页面不把试样自动升级为正式审美基线。
