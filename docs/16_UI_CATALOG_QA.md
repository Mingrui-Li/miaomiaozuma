# UI-07 · 百关视觉适配与逐关检查

2026-09-14开始，09-15继续；D022授权。独立场景 `assets/ui-catalog/ui-catalog.scene`，复用已验收UI组件和五猫图片。本阶段检查实际引擎里的百关布局，不改几何，不将静态猫链当作游戏通关证据。

## 查看与操作

- 本机静态测试入口：<http://127.0.0.1:62310/>，仅本机开放。
- Creator场景UUID：`b88a53a6-1366-4f99-8f6f-d431ccd25ad2`。编辑器预览：<http://localhost:7456/?scene=b88a53a6-1366-4f99-8f6f-d431ccd25ad2>，依赖编辑器已刷新新增场景。
- 工具栏提供上一关、下一关、开段/中段/末段、五猫/纸箱2与灰扑扑/纸箱1与灰扑扑/揭示、安全区、边界；100→1循环。URL加 `level=87` 可直接定位，编辑器URL用 `&level=87`。
- 暂停按钮、三道具选择与取消、中央猫交换仍接受实际触控；前五关不显示局内道具。静态样本不消耗库存、不记录存档、不进行真实发射或消除。

## 同源数据与绘制

`CatalogData.json`由唯一源 `design/track-review/catalog.source.json`只读派生，沿原有几何实现每2设计像素采样，保存六位小数；没有使用旧Paths或替换轨道。源SHA-256：`fab0b72c634d89b6b4081c16a4253c2b1046183308304091b8f0510e75c72e7c`。

前20关猫径64，后80关56，轨宽和间隔全部读源。为Graphics显示简化到最多225个折线点，偏差不超过0.25设计像素；原采样仍用于猫链定位、方向与避让。开/中/末段均是最多30只猫的占位样本，不是生成计划、难度曲线或时间进度。

入口、老巢各有一个位于原端点的深色洞口线，垂直原切线；箭头沿同一轨道的正向弧长绘制。两端设施使用已有80×40占位轮廓，中央窝半径52。设施尚未替换为正式猫窝/罐头美术，后续更大的轮廓、阴影和动画必须重新验算。

纸箱以方形、胶带及两次/一次受损状态区分；灰扑扑以灰色团块和问号遮住品种，揭示态恢复原猫。它们是确定性Graphics辨识试样，完整包络在原猫碰撞圆内，没有新增第六种基础猫。审查模式可在任意关开启障碍，不表示第一章有障碍，也不替代正式障碍投放配置。

## 静态检查结果

- 103项测试通过：100关各自的源采样、猫径、轨宽/间隔、包络、下一只、洞口方向、渲染误差和三尺寸热区；另含总表/源哈希、弧长插值和完整报告断言。
- HUD到玩法包络最小20设计像素；道具视觉上沿到包络最小26。360×640加模拟安全区后，实际暂停/道具热区最小余量约21.06/11.06设计像素，仍超过8。
- 下一只气泡最小余量约10.24，位于第3关。22关停靠顶部：23、26、29、46、53、56、58、61、62、65、67、73、78、82、84、85、86、87、90、95、97、100。位置固定，不随猫链或开/中/末段跳动。
- 完整逐关数值见 `design/ui/ui07/layout-audit.json`。设计像素不是手机物理像素；360短屏模拟时后期猫完整画布约20.90 CSS像素，细表情和受损小标记仍须手机辨识验证。

## 验证、文件与复现

新增 `CatalogBase.ts`（隔离UI容器）、`CatalogPreview.ts`（百关与试样）、`ReviewLayout.ts`（无cc的弧长/包络函数）、派生JSON及场景/meta。旧UI-05/UI-06场景和脚本不改；新字体是原OFL源的500字重子集，66,016字节，改名MMHW UI Catalog，覆盖清单与许可证另存。

```bash
# 以下命令使用Node22；不是系统旧版Node16
node tools/ui-catalog/generate-data.mjs
node --test tools/ui-catalog/audit.test.mjs
# Creator 3.8.8内置TypeScript
node /Applications/Cocos/Creator/3.8.8/CocosCreator.app/Contents/Resources/app.asar.unpacked/node_modules/typescript/bin/tsc --noEmit
```

`generate-data.mjs`遇到已存在且不同的派生数据会拒绝覆盖；不得借它更新源几何。字体工具为 `tools/ui-catalog/subset-font.py`。独立构建工具 `prepare-build.py`要求新的临时目录；`finalize-build.py`关闭默认性能浮层并附字体OFL/版权通知与空favicon。

浏览器通过Playwright CLI执行 `tools/ui-catalog/browser-check.js`，以实际Canvas点击切关和状态；尺寸分别为360×640、390×844、430×932，每尺寸重新加载。每关检查三个位置，截图对应短屏末段、常规中段、长屏开段；额外保存纸箱一次受损及揭示。最终浏览器结果与构建核对完成后记录在本节及 `design/ui/ui07/verification.json`，不以截图数量或CLI退出36单独判断通过。

09-15构建版三尺寸回归通过，共900个关卡/位置检查点；100→1和1→100边界、中央交换、暂停遮罩不透传、障碍受损/揭示通过，三份diagnostics均为空。实际截图和五张百关总览见 [截图索引](../output/playwright/ui07/README.md)。五张总览已逐格检查整体遮挡，放大复查第3/24/87/100关及短屏障碍状态；没有把总览缩略图当作手机小字可读性验收。

编辑器390×844同场景另300个检查点通过，diagnostics为空；合计四组回归。原尺寸逐关/额外状态截图共408张，按编辑器/构建及尺寸分目录保存。09-15重新执行103项测试和Creator严格编译仍通过；原source/meta及已验收UI文件哈希保持不变。

独立工程 `/private/tmp/mmhw-ui07-build-20260914`，构建退出36，实际运行另验。最终包复制至 `build/ui07-verified/`，57文件、15,589,764字节，服务62310仍指向同字节的临时构建目录。UI-06的本机测试入口保留，用户Creator不关闭。没有提交或推送。

## 已知边界

- 尚未进行本阶段的安卓手机、真实抖音胶囊、多指或热旋转测试。
- 没有动态猫链/合法发射/三消/回吸/百关可解性、真实广告、存档或支付验收；Q003危险阈值和呼噜公式没有在本阶段私自选值。
- 障碍、设施、背景和动画的正式美术未齐；现有图鉴/外观也没有在本任务扩产。无生图或付费API，不冒称使用Image2.5。
- 当前是带工具栏的debug web-mobile审查包，百关JSON约4.46MB，未做正式包体/分包/性能优化，不是可上线交付。
- 上次编辑器未即时索引新增场景，刷新后才可加载；构建版首次加载也出现较长等待。09-14截图又遇到自动审批额度耗尽，09-15继续补验。失败/空白截图不作为通过证据。
